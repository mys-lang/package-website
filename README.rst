Website
=======

- User account.
- Packages database.
- REST API.
- HTML.
- Serve everything on port 80.

GET /
GET /api/package/{name}-{version}.tar.gz
POST /api/package/{name}-{version}.tar.gz

mys run
